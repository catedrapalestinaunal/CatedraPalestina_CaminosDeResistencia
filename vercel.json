{
  "buildCommand": "npm run build:seo",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/og.tsx": {
      "runtime": "edge"
    }
  }
}
